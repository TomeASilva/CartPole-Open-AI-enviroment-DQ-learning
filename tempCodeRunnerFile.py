gamma = tf.constant(0.99, dtype=tf.float32, shape=None, name='gama')

# reward = tf.placeholder()

# features, labels = create_feature_outputs_tensor(5, 1)
# weights, biases = create_variables(4, [5, 25, 12, 1])
# output_pred = forward_propagation(features, weights, biases, [
#     tf.nn.relu, tf.nn.relu, None])
